#include "Headers/Main.h"

int main()
{
    // Rectangle rec(0,0,30,30,"yellow");
    // Rectangle rec1(5,5,10,10,"Green");
    // Circle c(14,10,14);
   
    // std::cout << std::boolalpha << rec1.within(rec.getX(), rec.getY(), rec.getWidth(), rec.getHeight());//rectangle within rectangle
    // std::cout << std::boolalpha << rec1.within(c.getX(),c.getY(),c.getRadius());//rectangle within circle
    
    SVG project;
    project.start();
    
}